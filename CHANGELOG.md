# Changelog

All notable changes to this project will be documented here.

## Unreleased

## v2.0.0 (planned)

- Tiered agent architecture (orchestrators → specialists → on-demand experts)
- Two-registry model: `_registry.md` (work done) + `_tech-debt.md` (deferred items)
- Protocol split for token efficiency: `SKILL.md` + `templates.md` + `reference.md` + `scripts/`
- Standard workflows for delegation (`/review-full`, `/ci`, `/debt`, `/postmortem`, `/rfc`)
- Project template includes report taxonomy folders + archive layout

## v1.0.0

- Initial “flat” multi-agent coordination setup with institutional memory

